import { PracticeMwMiddleware } from './practice-mw.middleware';

describe('PracticeMwMiddleware', () => {
  it('should be defined', () => {
    expect(new PracticeMwMiddleware()).toBeDefined();
  });
});
